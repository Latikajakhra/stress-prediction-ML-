# Stress Prediction Project

This project predicts stress levels using machine learning.

⚠️ Dataset is not included (lost), but the notebook shows full pipeline:

* Data preprocessing
* Model training
* Prediction

## How to run

Replace dataset path with your own CSV file:

```python
pd.read_csv("your_dataset.csv")
```
